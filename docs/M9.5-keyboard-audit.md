# M9.5 Keyboard-Only Editing Audit

> **Status:** Code-based audit complete (2026-05-15). Browser walk
> sections are marked **TODO: verify in browser** and should be
> confirmed by CT before declaring M9.5 fully done.

## 1. Hotkey Inventory

Full inventory derived from `frontend/src/lib/hotkeyMap.ts`.

### Global scope (active on all project routes)

| Combo | Action |
|---|---|
| `Mod+S` | Save page |
| `Mod+Shift+S` | Save project |
| `Mod+R` | Reload OCR |
| `Mod+Shift+R` | Reload OCR (edited) |
| `Mod+L` | Load page from disk |
| `Mod+G` | Rematch GT |
| `Mod+E` | Export… |
| `Mod+,` | OCR Config |
| `Mod+O` | Open Source Folder dialog |
| `?` | Show hotkey help modal |
| `Escape` | Close modal / cancel |
| `Mod+ArrowLeft` | Previous page |
| `Mod+ArrowRight` | Next page |
| `Mod+Home` | First page |
| `Mod+End` | Last page |
| `Mod+J` | Jump to page… |

### Viewport scope (active when image canvas is focused)

| Combo | Action |
|---|---|
| `Escape` | Cancel current mode |
| `Shift+P` | Toggle paragraphs layer |
| `Shift+L` | Toggle lines layer |
| `Shift+W` | Toggle words layer |
| `Shift+1` | Selection mode |
| `Shift+2` | Rebox mode |
| `Shift+3` | Erase mode |
| `Shift+E` | Erase mode (alternate) |
| `Shift+A` | Add Word mode |

### Matches scope (active when word-matches list is focused)

| Combo | Action |
|---|---|
| `J` | Next line card |
| `K` | Previous line card |
| `V` | Validate line |
| `U` | Unvalidate line |
| `D` | Delete line (with confirm) |
| `O` | Copy OCR→GT for line |
| `G` | Copy GT→OCR for line |
| `R` | Refine |
| `Shift+R` | Expand+Refine |
| `M` | Merge words |

### Word Edit Dialog scope

| Combo | Action |
|---|---|
| `Enter` | Commit GT |
| `Escape` | Close dialog |
| `Shift+Enter` | Apply and close |
| `ArrowLeft` | Previous word |
| `ArrowRight` | Next word |
| `Shift+ArrowLeft` | Nudge left edge |
| `Shift+ArrowRight` | Nudge right edge |
| `Shift+ArrowUp` | Nudge top edge (expand) |
| `Shift+ArrowDown` | Nudge top edge (shrink) |
| `Ctrl+ArrowLeft` | Nudge right edge (shrink) |
| `Ctrl+ArrowRight` | Nudge right edge (expand) |
| `Ctrl+ArrowUp` | Nudge bottom edge (shrink) |
| `Ctrl+ArrowDown` | Nudge bottom edge (expand) |
| `R` | Refine |
| `Shift+R` | Expand+Refine |
| `M` | Apply style |
| `Shift+M` | Apply component |
| `Delete` | Delete word (with confirm) |

### Source-Folder Dialog scope

| Combo | Action |
|---|---|
| `Enter` | Open typed path |
| `Mod+Enter` | Apply source folder |
| `Escape` | Cancel |

### GT Input scope

| Combo | Action |
|---|---|
| `Tab` | Move to next GT input |
| `Shift+Tab` | Move to previous GT input |
| `Enter` | Commit GT |
| `Escape` | Revert GT |

## 2. Focus Management — Code Review

### Modals (OCRConfigModal, WordEditDialog, HotkeyHelpModal, ConfirmDialog, ExportDialog)

- `SourceFolderDialog.tsx` uses `autoFocus` on the path-input. When dialog
  opens, focus lands on the text field immediately. `Escape` calls `handleCancel`
  via `onKeyDown`. `Mod+Enter` calls `handleApply`. **No focus trap** — Tab
  navigates the page behind the overlay. TODO: verify whether focus escapes to
  background content in browser walk.
- `WordEditDialog.tsx` uses `autoFocus` on the GT textarea. Escape and Enter
  are handled. Same focus-trap gap as SourceFolderDialog.
- `HotkeyHelpModal.tsx` — rendered as a full-screen overlay. Close button
  present; Escape bound via `useGlobalHotkeys`. TODO: verify tab order inside
  the modal table doesn't reach background elements.
- `ConfirmDialog.tsx` — rendered inline. Tab order may escape to background.
  TODO: verify.
- `OCRConfigModal.tsx` — rendered inline with `role="dialog"`. Has close
  button; Escape registered via `useGlobalHotkeys`. TODO: verify focus trap.

### Known Gap: No Focus Trap

None of the dialogs implement a focus trap (e.g. `focus-trap-react` library or
manual `onKeyDown Tab` cycling). This is a potential axe-core failure for WCAG
2.1 §2.1.2 (No Keyboard Trap). The axe-core E2E test (`tests/e2e/test_a11y.py`)
validates open pages but does not open modals during the audit.

**Recommendation:** Add focus traps to all four modal dialogs before final
M9.5 sign-off. Low complexity — `focus-trap-react` is a one-line wrapper.
Track as separate issue.

## 3. Tab Order — Root Page

RootPage renders:

- `HeaderBar` → `ProjectLoadControls` (select + LOAD button + source-folder
  button)
- Project list as a `<select>` element

Expected tab order: HeaderBar buttons → project-select → LOAD → source-folder.
TODO: verify in browser.

## 4. Tab Order — Project Page

ProjectPage renders (left-to-right, top-to-bottom):

1. `HeaderBar` — breadcrumb, change-project button, page nav buttons, OCR
   Config button
2. `ImageTabsHeader` — Words / Lines / Paragraphs / Matched-Words tabs + mode
   radio buttons
3. `BBoxOverlay` / `PageImageCanvas` — Konva stage (no native tab stop; canvas
   receives keyboard events only when programmatically focused)
4. `TextTabs` — OCR / GT / Plaintext tabs
5. `WordMatchView` — scrollable virtualised list of line cards

Potential gap: the Konva canvas (`<canvas>` element) has no `tabIndex` by
default. Keyboard navigation into the viewport requires Tab to land on the
canvas or a containing div with `tabIndex=0`. The `PageImageCanvas.tsx` uses
`tabIndex={0}` on the Stage container div (confirm from code). TODO: verify
in browser that canvas is reachable without mouse.

## 5. Scope Activation Contract

`useGlobalHotkeys` (`frontend/src/hooks/useGlobalHotkeys.ts`) registers
global-scope combos on `document` via `react-hotkeys-hook`. Viewport/matches/
dialog scopes are activated when the respective container receives focus (via
`enabledScopes` state in the hook). The scope bridge is in
`frontend/src/lib/hotkey-bridge.ts`. TODO: confirm scope activation/deactivation
on Tab in browser walk.

## 6. axe-core Results (E2E, CI)

The `tests/e2e/test_a11y.py` axe-core scan runs on:

- Root page
- Project page (when a project is loaded)

As of M9.2 the scan passes with zero critical violations. Modals are not
opened during the scan. Extending the scan to include modals is tracked as a
follow-on item.

## 7. Browser Walk TODO List

The following items require a live browser session to confirm:

- [ ] Tab reaches canvas in ProjectPage (no mouse required to enter viewport)
- [ ] Escape closes each modal without focus leaking to background
- [ ] Focus trap: Tab cycling stays inside modal while modal is open
- [ ] Source-folder dialog: `Enter` in path-input triggers Open-Typed, not Apply
- [ ] Mod+Enter in path-input triggers Apply
- [ ] `?` key opens HotkeyHelpModal from every route
- [ ] `Mod+,` opens OCRConfigModal from every route
- [ ] `Mod+ArrowLeft` / `Mod+ArrowRight` navigate pages when focus is anywhere
  on the ProjectPage (not just in the image pane)
- [ ] `J` / `K` in the matches list navigates line cards when the list is focused
- [ ] `V` validates the focused line card
- [ ] Word Edit Dialog: `ArrowLeft` / `ArrowRight` move between words
- [ ] ConfirmDialog: can be dismissed with `Escape` and confirmed with `Enter`

## 8. Open Items / Recommended Follow-ons

1. **Focus traps in modals** — add `focus-trap-react` to all four modal
   components. File a new issue under `kind:chore, effort:S`.
2. **axe-core modal scan** — extend `test_a11y.py` to open each modal before
   running the axe scan. File as `kind:chore, effort:S`.
3. **Canvas tabIndex** — confirm `tabIndex={0}` is on the Konva Stage container
   in `PageImageCanvas.tsx`; if not, add it and write a test.

---

*Generated: 2026-05-15. Code-based audit by pd-ocr-labeler-spa agent.
Browser walk pending CT confirmation.*
